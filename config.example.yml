api:
  base_url: "http://localhost:8000/v1"
  model: "your-model-name"
  concurrency: 8  # max simultaneous requests (default: 8)

generation:
  max_tokens: 100
  temperature: 0.1
  top_p: 0.9
  repetition_penalty: 1.1
  seed: 42

input:
  prompt_field: "prompt"
